Housekeeping for the marketplace listing.

## Fixed
- `manifest.json`, `package.json` and the helper all reported 0.2.0 while the packaging said 0.3.4.
  Every version string now matches the tag

## Added
- An "Install the helper" button in the panel when the helper is missing. It opens a terminal
  running the documented commands, so nothing installs silently
- The alien power key on the keyboard map is the same height as the keys around it
- CI fails when the version strings disagree or a release note is missing
- The release workflow opens the marketplace verification issue and prints the checksum line for
  `packaging/bin/PKGBUILD`
