Maintenance. No user facing changes.

## Fixed
- The fan curve test waited on the CPU fan write and then asserted on the GPU fan, which the curve
  loop writes second, so it could fail on a loaded machine. It waits for both now
- `npm test` passed a directory to `node --test`, which needs a newer node than the one CI pins.
  It is a shell expanded glob now, and still keeps the clone that makepkg leaves in `packaging/`
  out of the run
