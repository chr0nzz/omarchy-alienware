Maintenance. No user facing changes.

## Changed

- Test tooling only. The fan curve test no longer races its own writes, and the test
  runner is invoked in a way that works on the node version CI pins
- Functionally identical to 0.3.1
